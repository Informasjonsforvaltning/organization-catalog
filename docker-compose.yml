version: "3"

services:
  organization-catalogue:
    image: eu.gcr.io/fdk-infra/organization-catalogue:latest
    depends_on:
      - mongodb
    restart: always
    environment:
      - MONGO_USERNAME
      - MONGO_PASSWORD
      - ENHETSREGISTERET_PROXY_URL
    ports:
      - 8140:8080

  mongodb:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin
    ports:
      - 27017:27017

  nginx-enhetsregisteret-proxy-mock:
    image: eu.gcr.io/fdk-infra/nginx-enhetsregisteret-proxy-mock:env_ut1_latest
    ports:
      - "8080:8080"
