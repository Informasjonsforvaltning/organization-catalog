apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    fdk.service: organization-catalog
  name: organization-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      fdk.service: organization-catalog
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        fdk.service: organization-catalog
    spec:
      containers:
        - name: organization-catalog
          env:
            - name: POSTGRESQL_HOST
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRESQL_HOST
            - name: POSTGRESQL_DB
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: POSTGRESQL_DB
            - name: POSTGRESQL_USER
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: POSTGRESQL_USER
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: POSTGRESQL_PASSWORD
            - name: KEYCLOAK_OIDC_ISSUER
              valueFrom:
                secretKeyRef:
                  name: keycloak
                  key: KEYCLOAK_OIDC_ISSUER
            - name: ENHETSREGISTERET_PROXY_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: commonurl
                  key: ENHETSREGISTERET_PROXY_BASE_URI
            - name: ORGANIZATION_CATALOG_HOST
              valueFrom:
                secretKeyRef:
                  name: commonurl
                  key: ORGANIZATION_CATALOG_BASE_URI
            - name: ENTUR_TRANSMODEL_URL
              valueFrom:
                secretKeyRef:
                  name: commonurl
                  key: ENTUR_TRANSMODEL_URL
            - name: ENTUR_HEADER_KEY
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: ENTUR_HEADER_KEY
            - name: ENTUR_HEADER_VALUE
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: ENTUR_HEADER_VALUE
            - name: CORS_ORIGIN_PATTERNS
              valueFrom:
                secretKeyRef:
                  name: organization-catalog
                  key: CORS_ORIGIN_PATTERNS
          image: organization-catalog
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "2000Mi"
              cpu: "10m"
            limits:
              memory: "2500Mi"
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 5
      restartPolicy: Always
